# 5-17

![5-17](https://s2.loli.net/2025/11/04/LPfIatdM3SFzTBq.png)

assignment:
    1. 实现一个更有效的版本

tips:
    1. 不调用库函数
    2. 对仍以n都能工作，不是K（sizeof unsigned long）时，使用循环展开时完成最后几次迭代
    3. 未对齐的写操作进行优化。写出这样的代码，开始时直到目的地址是 K 的倍数时，使用字节级的写，然后进行字级的写，（如果需要）最后采用用字节级的写
    4. 注意cnt 足够小以至于一些循环上界变成负数的情况。对于涉及 sizeof 运算符的表达式，可以用无符号运算来执行测试。（参见 2.2.8 节和家庭作业 2.72）

## Answer

见[5.17.c](5.17.c)

eazy peasy
